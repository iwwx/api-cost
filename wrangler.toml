name = "api-cost"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# KV Namespace 绑定通过 Cloudflare Dashboard 管理:
# Settings → Functions → KV namespace bindings
# Variable name: USER_DATA
# KV namespace: api-cost-user-data
